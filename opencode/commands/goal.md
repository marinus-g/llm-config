---
description: "Set a completion condition — agent keeps working until the evaluator confirms it's met. Usage: /goal <condition> [--max-turns N] [--interval <Ns|Nm|Nh>] | /goal (status) | /goal clear | /goal pause | /goal resume. Without --interval: immediate retries (coding). With --interval: paced polling (monitoring)."
---

$ARGUMENTS
